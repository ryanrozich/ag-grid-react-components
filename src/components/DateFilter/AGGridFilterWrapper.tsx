import DateFilter from "./index";

/**
 * Direct export of DateFilter for AG Grid integration
 * The DateFilter component already handles AG Grid integration internally
 */
export const AGGridFilterWrapper = DateFilter;

export default AGGridFilterWrapper;
